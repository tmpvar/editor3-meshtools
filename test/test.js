var editor3-meshtools;
if (typeof require !== "undefined") {
  editor3-meshtools = require("../editor3-meshtools.js");
} else {
  editor3-meshtools = window.editor3-meshtools;
}

var ok = function(a, msg) { if (!a) throw new Error(msg || "not ok"); };
var eq = function(a, b) { if (a!==b) throw new Error(a + " !== " + b); };

describe('editor3-meshtools', function() {
  describe('#', function() {
    it('', function() {
      
    });
  });
});
